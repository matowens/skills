interface:
  display_name: "Mat Init"
  short_description: "Interview and initialize a project workflow"
  default_prompt: "Use $mat-init to inspect this repository, interview me about its project-specific context, and initialize Mat's private workflow."
policy:
  allow_implicit_invocation: false
