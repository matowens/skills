interface:
  display_name: "Mat Build"
  short_description: "Build or correct one approved task"
  default_prompt: "Use $mat-build to implement the next approved Task or apply review corrections to the current Task."
policy:
  allow_implicit_invocation: false
