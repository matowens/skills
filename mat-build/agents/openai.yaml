interface:
  display_name: "Mat Build"
  short_description: "Build and validate one approved task"
  default_prompt: "Select the mat-build skill to implement and validate the approved task."
policy:
  allow_implicit_invocation: false
